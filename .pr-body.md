## Summary
- Rebased onto `main` (1.5.14) and add portrait-native **720×1280** mobile virtual canvas with uniform scaling (no letterboxing or font stretch).
- Retune mobile chrome (`HOME` pill + circular back), menus, Reading Snake, Math Orbit, and Math Invaders HUDs for portrait layout.
- Tap-to-select title/mission menus; mobile text scale 1.75×; root `build-wasm.ps1` with WASM freshness verification.
- Security: sanitize buildinfo paths, tighten `postMessage` target origin, document CSP and audit in README.
- Bump build to **1.5.15** and update README/docs.

## Test plan
- [x] Portrait title / mission select: readable menus, HOME pill, tap-to-select
- [x] Reading Snake: definition card and board layout on 720×1280
- [x] Math Orbit: HUD, question, paddle, chrome row
- [x] `cargo test` (17 tests) and WASM build pass
- [x] `build-wasm.ps1 -VerifyOnly` passes; buildinfo has no absolute paths
- [x] 32/32 iPhone screenshot audit (`target/iphonescreenshots/5_23_2026_18_34`)
