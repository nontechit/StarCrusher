## Summary
- Fix portrait touch mapping by converting screen coordinates through the virtual 1280x720 camera in `screen.rs`, so paddle and ship controls align with finger position on iPhone Safari.
- Restructure Math Orbit mobile HUD: question card at top, wider numbered targets, readable `Q`/`Lives`/`Score` stat pills, and clearer footer copy above `START`.
- Restructure Math Invaders mobile HUD: question-first header, stat pills for wave/score/lives, lowered enemy field, and 1.45x portrait scale for ship and numbered targets.
- Move the mobile web Site button to the top-right shell control area.
- Cache-bust the checked-in WASM URL in the static shell so the deployed game picks up mobile UI changes promptly.
- Replace Reading Snake portrait D-pad with tap-or-swipe steering on the board; clean up the header, stat pills, goal banner, and larger high-contrast board layout.
- Bump build to 1.5.6 and update README/docs.

## Test plan
- [x] Math Orbit (portrait): question at top; widened number targets and stat pills are readable; footer and `START` do not overlap
- [x] Math Invaders (portrait): question at top; stat pills, ship, and targets are readable; Site button does not overlap in-canvas HUD
- [ ] Reading Snake (portrait): tap and swipe board steering work; no arrow buttons; header/goal/banner/board do not overlap
- [ ] Desktop landscape: keyboard controls unchanged
- [x] `cargo test` and WASM build pass
