## Summary
- Refine portrait mobile layout after full iPhone review: remove active mobile `HOME`/`X` chrome, enlarge title rows/helper copy, and keep desktop `Back to Home`.
- Expand the Math Invaders question card, lower stat pills and gameplay elements, enlarge the player ship, and make enemies step closer after each kill.
- Preserve tap-to-select title/mission menus with hitboxes aligned to the visible mobile rows.
- Security/docs: verify no private screenshot files are queued, run dependency audit, and document the current audit state in README.
- Rebuild checked-in `star-crusher.wasm` and update README/docs.

## Test plan
- [x] Portrait title / mission select: larger readable rows, no mobile HOME/X overlap, tap-to-select
- [x] Reading Snake: definition card and board layout remain clean on 720x1280
- [x] Math Orbit and Math Invaders: HUD/question cards, stat rows, gameplay spacing reviewed
- [x] `cargo test` (17 tests), `cargo clippy --all-targets`, and WASM build pass
- [x] `build-wasm.ps1 -VerifyOnly` passes; buildinfo has no absolute paths
- [x] `cargo audit` completed with the existing allowed Macroquad warning (`RUSTSEC-2025-0035`) and no unallowed vulnerability failure
- [x] 32/32 iPhone screenshot audit (`target/iphonescreenshots/5_24_2026_0_27`)
