## Summary
- Remove the global web touch-bridge keypad intercept so right-side taps reach Math Orbit, Math Invaders, Reading Snake, and gate screens instead of firing stray `Enter`/digit keys.
- Math Orbit on iPhone separates aim from launch: first gameplay-band tap moves the paddle, a second tap or `START` launches the ball.
- Gate questions on portrait mobile submit only through the on-screen `OK` pad tap; desktop keyboard submit is unchanged.
- Bump build to 1.5.10 and update README/docs for the revised mobile touch behavior.

## Test plan
- [ ] Math Orbit (portrait): full-width paddle drag works; first tap aims only; second tap or `START` launches
- [ ] Math Invaders (portrait): right-side lower-screen move/fire works; gate `OK` submits; empty bottom band does not submit
- [ ] Reading Snake (portrait): tap and swipe on the right board edge steer correctly
- [ ] Desktop landscape: keyboard controls unchanged for Math Orbit, Math Invaders, gates, and Reading Snake
- [x] `cargo test` and WASM build pass
